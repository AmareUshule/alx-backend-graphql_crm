# CRM Celery Report Setup

## Requirements
- Redis server running at `redis://localhost:6379/0`
- Python dependencies installed: `pip install -r requirements.txt`

## Steps
1. Run Django migrations:
   ```bash
   python manage.py migrate

